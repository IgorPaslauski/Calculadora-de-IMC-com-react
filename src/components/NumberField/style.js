import { StyleSheet} from 'react-native';
const styles = StyleSheet.create({
    label: {
      fontSize: 18,
      color: '#000',
      fontWeight: 'bold',
      marginBottom: 8,
    },
    input: {
      borderWidth: 1,
      borderColor: '#000',
      borderRadius: 8,
      padding: 8,
      fontSize: 16,
      color: '#000',
      marginBottom: 16,
    }
  });

export default styles;